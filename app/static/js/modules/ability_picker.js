(function initabilitypickerModule(globalScope) {
  globalScope.SZOPModules = globalScope.SZOPModules || {};
  globalScope.SZOPModules['ability_picker'] = {
    init: function initPassthrough() {
      return null;
    },
  };
}(window));
