(function initrostereditorModule(globalScope) {
  globalScope.SZOPModules = globalScope.SZOPModules || {};
  globalScope.SZOPModules['roster_editor'] = {
    init: function initPassthrough() {
      return null;
    },
  };
}(window));
