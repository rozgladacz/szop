(function initweaponpickerModule(globalScope) {
  globalScope.SZOPModules = globalScope.SZOPModules || {};
  globalScope.SZOPModules['weapon_picker'] = {
    init: function initPassthrough() {
      return null;
    },
  };
}(window));
