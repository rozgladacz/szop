(function initarmorytreeModule(globalScope) {
  globalScope.SZOPModules = globalScope.SZOPModules || {};
  globalScope.SZOPModules['armory_tree'] = {
    init: function initPassthrough() {
      return null;
    },
  };
}(window));
