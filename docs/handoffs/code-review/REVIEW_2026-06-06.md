# Code Review — 2026-06-06

**SHA HEAD:** `3215f38`
**Branch:** `Faza_A`
**Zakres:** `fb0ffa9..HEAD` (6 commitów od poprzedniego review)
**Effort:** high (inline multi-angle, autonomiczna sesja codzienna)

## Pliki w diffie (`fb0ffa9..HEAD`, tylko `app/`)

```
app/rulesets/v1/abilities.yaml  | 125 +++  (rozkaz_tak field × 88 + reflow opisów)
app/services/engine/combat.py   |  11      (R5.c kontratak Wyczerpany skip gdy charger pokonany)
app/services/engine/effects.py  |  30      (R5.x INCOMPLETE_ABILITIES + dywersant/zguba)
app/services/engine/events.py   |  28      (R5.e MutexCollision event type #14)
app/services/engine/phases.py   | 246      (R5.d/e/f/g — regroup trigger, mutex, terrain, deploy Ufort)
app/services/engine/reducers.py |  21      (R5.e _reduce_mutex_collision)
app/services/engine/state.py    |  24      (R5.a Lokalizacja enum + UnitBlob.location)
app/services/rulesets/models.py |   4      (rozkaz_tak Pydantic field)
```

Commits: `41d2a8a` (R4.rozkaz_tak + R5.a/c/f), `ee8975f` (R5.d/x), `203daac` (R5.g Niebezpieczny), `3215f38` (R5.e mutex).

## Stan testów

- `pytest -q` → **1419 passed** (pełny suite, 0 fail).
- Migration/engine focused (258) → pass; YAML reflow opisów folduje się poprawnie (test_abilities_migration exact-match przechodzi).

Brak crashy / regresji w testach. Poniższe findings to **semantyka reguł**, nie awarie — wszystkie wymagają decyzji autora reguł (drift), więc NIE auto-naprawione.

## Wyniki code-review

```json
[
  {
    "file": "app/services/engine/phases.py",
    "line": 349,
    "summary": "Rany z niebezpiecznego terenu (R5.g) dopisywane są surowo do wounds_received bez emisji ModelKilled — modele nigdy nie giną od terenu",
    "failure_scenario": "_apply_maneuver dodaje `moved.wounds_received += wounds_inflicted` i emituje wyłącznie EffectApplied(niebezpieczny). W modelu ran silnika (reducers.py:90-159, combat.py:_allocate_wounds) wounds_received to PULA MARKERÓW, a faktyczne zabicie modelu wymaga osobnego kroku: ModelKilled event (replay) lub pętla kill w _allocate_wounds (live), która dekrementuje models_alive i odejmuje toughness_per_model. Ścieżka terenu nie robi ani jednego — emituje 0× ModelKilled i nie uruchamia pętli alokacji. Skutek: oddział 5 modeli × toughness 3 wrzucający np. 6 jedynek dostaje wounds_received=6 (2 modele warte ran), ale models_alive pozostaje 5. Liczba ataków, liczba kości w kolejnym teście terenu i siła oddziału liczą się z pełnych modeli, mimo śmiertelnych ran. Komentarz (phases.py:320) zakłada 'alokację standardową w przyszłej aktywacji', ale w silniku NIE istnieje faza drenująca surowe markery wounds_received na zabicia (kill pochodzi tylko z ModelKilled emitowanych przy walce). Markery zostają permanentnie. Częściowo maskowane: current_toughness = models_alive*tpm - wounds_received obniża 'efektywną' wytrzymałość dla testu połowy siły (pkt 20.b), ale models_alive — sterujący resztą mechaniki — pozostaje błędnie wysoki."
  },
  {
    "file": "app/services/engine/phases.py",
    "line": 351,
    "summary": "Ruch przez niebezpieczny teren wymusza test Przegrupowania na poruszającym się oddziale (received>0, dealt=0 → received>dealt)",
    "failure_scenario": "Nowy trigger pkt 20.a (R5.d) to `delta_received <= delta_dealt → skip`. Manewr w niebezpieczny teren dodaje rany do wounds_received aktora (delta_received>0) i nie zadaje żadnych ran (Manewr nie emituje ShotResolved/MeleeResolved → delta_dealt=0). Aktor jest zawsze w regroup_subjects, więc `delta_received(>0) <= 0` = False → test Przegrupowania się odpala. Oddział może zostać Wyczerpany/Przyszpilony (a przy 3 porażkach pokonany) wyłącznie za przejście przez niebezpieczny teren — bez wymiany ognia/wręcz. Pre-drift trigger (`delta>0 OR melee_balance<0`) miał ten sam efekt dla ran, ale warto potwierdzić z autorem reguł czy samo-zranienie od terenu (pkt 4.c.v) ma liczyć się jako 'otrzymał > zadał' w pkt 20.a. Jeśli nie — teren powinien być wykluczony z dealt/received bilansu (np. osobna pula nie-bojowa)."
  },
  {
    "file": "app/services/engine/phases.py",
    "line": 284,
    "summary": "deployment_round nadaje Ufortyfikowany WSZYSTKIM blobom z models_alive>0, w tym rezerwom off-board (ZAPLECZE: Zasadzka/Rezerwa) które nie są 'rozstawione' w rundzie 1",
    "failure_scenario": "Pkt 13.c (R5.f) mówi o KAŻDYM *rozstawionym* oddziale. Pętla iteruje po `current.blobs` filtrując tylko `models_alive>0`, bez sprawdzenia `location != Lokalizacja.ZAPLECZE` (pole dodane w tym samym zakresie, R5.a). Jednostki Zasadzka/Rezerwa, które rozstawiają się dopiero później, dostają Ufortyfikowany już w rundzie 1 będąc poza planszą. W praktyce nisko-szkodliwe (off-board nie da się zaszarżować, a Ufort zdejmowany na starcie własnej aktywacji), ale to stan niespójny z semantyką pkt 26 (ZAPLECZE = poza grą) i potencjalny błąd gdy przyszła logika będzie liczyć Ufortyfikowany na rezerwach. Fix: dodać `and b.location != Lokalizacja.ZAPLECZE` do warunku."
  }
]
```

## Sugerowane akcje

- **[Poprawność — model ran]** `phases.py:349`: Ścieżka Niebezpieczny terenu powinna albo (a) uruchomić tę samą pętlę kill co `_allocate_wounds` i emitować `ModelKilled` per zabity model, albo (b) świadomie udokumentować, że teren tworzy WYŁĄCZNIE markery, ze wskazaniem KONKRETNEJ fazy, która później zamienia surowe `wounds_received` na zabicia. Obecnie takiej fazy brak → modele nie giną od terenu. **Należy do R5.g — wpisać do HANDOFF_faza-b-rules-resync.**

- **[Semantyka reguł — do potwierdzenia]** `phases.py:351`: Czy rany od niebezpiecznego terenu (samozranienie) mają wyzwalać test Przegrupowania (pkt 20.a received>dealt)? Jeśli nie — wykluczyć teren z bilansu received. Decyzja autora reguł.

- **[Higiena — ZAPLECZE guard]** `phases.py:284`: Dodać filtr `b.location != Lokalizacja.ZAPLECZE` przy nadawaniu Ufortyfikowany w deployment_round, żeby rezerwy off-board nie były fortyfikowane.

## Uwagi proceduralne

- Untracked artefakty w working tree (`_audit_diff.txt`, `_b39_diff*.txt`, `_drift_*.diff`, `_engine_diff.txt`) — pliki pomocnicze poprzednich sesji (Jun 2-3), per HANDOFF nie commitowane. Pozostawione bez zmian (zasada higieny: nie usuwać cudzych plików).
- Working tree poza tym czysty (jedynie `.claude/settings.local.json` 2 linie). Brak innych zmian do review.
- Nie wykryto żadnego crash-buga ani regresji testowej; wszystkie 3 findings to świadome decyzje driftu R5.d/f/g wymagające domknięcia semantyki.
