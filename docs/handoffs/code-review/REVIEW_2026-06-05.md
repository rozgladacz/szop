# Code Review — 2026-06-05

**SHA HEAD:** `fb0ffa9`
**Branch:** `Faza_A`
**Effort:** medium (high — 7 angles × 6 candidates → 1-vote verify)

## Pliki w diffie (HEAD~1..HEAD)

```
app/services/rulesets/cost_functions.py | 56 +++++++++++++++++++++++++++++++++
app/services/rulesets/handlers.py       | 56 +++++++++++++++++++--------------
tests/test_cost_functions.py            | 46 +++++++++++++++++++++++++++
3 files changed, 135 insertions(+), 23 deletions(-)
```

**Commit:** `faza-b-rules-resync R3: T_eff w cost_functions + handlers aura/order_like`

---

## Wyniki code-review

```json
[
  {
    "file": "app/services/rulesets/handlers.py",
    "line": 158,
    "summary": "Falsy-zero guard silently replaces toughness=0 with the t_eff default of 6.0",
    "failure_scenario": "Line 158 (and mirror at line 198): `carrier = ctx.toughness if ctx.toughness else None`. `_HandlerCtx.toughness` is typed `float` with no lower-bound constraint; an explicit `toughness=0` argument produces `ctx.toughness=0.0`, which is falsy, so `carrier` becomes `None`. `t_eff(tables, None)` then substitutes 6.0 and returns T_eff=8.0 instead of the correctly-clamped `max(8, 4/3*0)=8.0`. Numerically coincidental today, but any future change to `t_eff_clamp_min` below ~1.33 or any caller treating 0 as a meaningful value will silently produce wrong pricing. Fix: `carrier = ctx.toughness if ctx.toughness is not None else None`, or simply `carrier = ctx.toughness` since `_HandlerCtx.toughness` is already guaranteed float."
  },
  {
    "file": "app/services/rulesets/handlers.py",
    "line": 206,
    "summary": "_order_like_cost mistrzostwo path now multiplies by T_eff (carrier-scaled), diverging from the old flat multiplier of 10.0 for any non-default toughness",
    "failure_scenario": "Old: `_mistrzostwo_aura_cost(w_slug) * 10.0`. New: `_mistrzostwo_aura_cost(w_slug) * t_eff(tables, carrier, extra=order_bonus)`. For a carrier with toughness=9: `t_eff = clamp(4/3*9, 8, 24)+2 = 12+2 = 14` vs old 10 — a 40% cost increase. The commit docstring says backward-compatibility holds only for the `toughness=6` legacy default. Any existing roster unit with a `rozkaz: mistrzostwo:<weapon>` ability and toughness above 6 will receive a silently inflated cost post-resync. Verify intent against oracle `abilities.py:375-382` to confirm whether the mistrzostwo special-case should also scale with carrier toughness or stay at the flat ordinal tier."
  },
  {
    "file": "app/rulesets/v1/ability_costs.yaml",
    "line": 141,
    "summary": "Dead YAML fields inner_tou, range_12_multiplier, aura_mistrzostwo_multiplier (aura handler) and inner_tou, mistrzostwo_multiplier (order_like handler) are silently ignored by the new handlers",
    "failure_scenario": "The new `_aura_cost` and `_order_like_cost` no longer call `getattr(spec, 'inner_tou', ...)` or any of the four removed spec attributes. A game designer who reads the YAML comment on line ~139 and edits `inner_tou: 12.0` to tune aura pricing will observe zero effect at runtime with no error. The stale fields should be removed (or marked `# DEPRECATED — superseded by tables.aura_order_formula`) and the YAML comment updated."
  },
  {
    "file": "app/services/rulesets/handlers.py",
    "line": 391,
    "summary": "Contradictory toughness defaults: ctx constructor uses 1.0 for missing toughness, while t_eff's None-path uses 6.0; they diverge under any custom ruleset with t_eff_clamp_min < 1.34",
    "failure_scenario": "When `ability_cost_components_yaml(toughness=None)` is called, line 391 stores `1.0` in `ctx.toughness`. The falsy-check at line 158 passes `carrier=1.0` to `t_eff`. Under a custom ruleset with `t_eff_clamp_min: 1` (valid per `ge=1`), `t_eff(tables, 1.0)` = `4/3 * 1.0 = 1.33`, while the intended legacy compat path `t_eff(tables, None)` = `t_eff(tables, 6.0)` = `8.0`. The two defaults silently diverge. Either the ctx default should match the t_eff default (both 6.0), or the t_eff None-path is dead and should be documented as unreachable."
  },
  {
    "file": "app/services/rulesets/cost_functions.py",
    "line": 221,
    "summary": "t_eff() duplicates the _aura_eff_tou closure in abilities.py with no shared code; formula parity is structurally unenforced",
    "failure_scenario": "`abilities.py:_aura_eff_tou` (closure, lines 315-316) and `cost_functions.t_eff` (lines 228-253) implement the same `clamp(factor * tou, cmin, cmax) + extra` formula with identical hard-coded defaults (4/3, 8, 24). They cannot currently share code because `_aura_eff_tou` is a non-importable closure. If someone changes the formula in `abilities.py` (e.g. raises the clamp ceiling), `t_eff` will silently diverge. Recommend promoting `_aura_eff_tou` to a module-level pure function in `abilities.py` (or a new `primitives` helper) and having both callers import it."
  },
  {
    "file": "app/services/rulesets/handlers.py",
    "line": 163,
    "summary": "extract_number fallback in the mistrzostwo aura branch returns 0.0 (not 6.0) for non-numeric input, silently suppressing the range-12 bonus",
    "failure_scenario": "`aura_range = extract_number(parts[1]) if len(parts) == 2 else 6.0`. When `parts[1]` contains no digits, `extract_number` returns `0.0`. Then `abs(0.0 - 12.0) < 1e-6` is `False`, so `extra=0.0` and the range-12 bonus is silently lost. The non-mistrzostwo path uses `parse_aura_value` which applies an `or 6.0` fallback, producing a 6\" aura (not 0\"). Malformed YAML like `mistrzostwo:sword|range_extended` would under-price the aura without any error or warning."
  }
]
```

---

## Sugerowane akcje

- **[PILNE — poprawność]** `handlers.py:158,198`: Zmień `carrier = ctx.toughness if ctx.toughness else None` na `carrier = ctx.toughness` (lub `if ctx.toughness is not None`). Falsy-zero bug. Najprostszy fix: skoro `_HandlerCtx.toughness` jest zawsze `float`, przekazuj wprost do `t_eff`.

- **[PILNE — weryfikacja reguł]** `handlers.py:206`: Sprawdzić intencję formuły `_order_like_cost` mistrzostwo — czy T_eff-scaling jest prawidłowy dla zdolności `mistrzostwo:` w rozkazu/klątwy? Porównać z oracle `abilities.py:375-382`. Jeśli mistrzostwo powinno być flat tier (jak przed resyncem), oddzielić ścieżkę mistrzostwo od `t_eff`.

- **[Higiena YAML]** `ability_costs.yaml:141-143,156-157`: Usunąć (lub oznaczyć jako `# DEPRECATED`) pola `inner_tou`, `range_12_multiplier`, `aura_mistrzostwo_multiplier`, `mistrzostwo_multiplier` — są martwymi danymi ignorowanymi przez nowe handlery. Zaktualizować komentarze YAML.

- **[Spójność domyślnych]** `handlers.py:391` + `cost_functions.py:250`: Ujednolicić domyślne dla brakującej wytrzymałości — albo `_HandlerCtx` używa `6.0` (zgodnie z oracle), albo `t_eff` None-path jest udokumentowany jako nieosiągalny. Obecnie `1.0` vs `6.0` zachowuje się tak samo tylko przy domyślnym `clamp_min=8`.

- **[Architektura]** `cost_functions.py:221`: Rozważyć wyciągnięcie `_aura_eff_tou` z closury w `abilities.py` do funkcji modułowej (lub helpera w `primitives`), żeby `t_eff` mogło ją importować zamiast duplikować. Eliminuje ryzyko driftu formuły między dwiema implementacjami.
