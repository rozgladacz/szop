# HANDOFF — rozmiar-podstawki

> **Wątek:** Nowa cecha oddziału „rozmiar podstawki" (mała/średnia/duża) ze zniżką kosztu broni wręcz dla modeli-nadmiaru w dużych oddziałach (wpływa na klasyfikację Wojownik/Strzelec) + pole „Walczące modele" w Stanie Bitewnym.
> **Status:** In progress
> **Utworzony:** 2026-07-14
> **Ostatnia aktualizacja:** 2026-07-14

## Cel
Silnik kosztów ma dziś wyceniać każdy model tak, jakby cała jego broń wręcz zawsze mogła walczyć — w dużych oddziałach to nierealistyczne, bo tylko część modeli dosięga wroga. Wprowadzamy `Unit.base_size` (mała/średnia/duża) z limitami liczby skutecznie walczących modeli, sortujemy modele oddziału (+dołączeni bohaterowie) malejąco po koszcie broni wręcz i stosujemy mnożnik 1.0/0.5/0.1 wg pozycji. Zniżka liczona jest PRZED klasyfikacją Wojownik/Strzelec (wpływa na nią). Dodatkowo w Stanie Bitewnym, trybie „Wręcz", pole „Walczące modele" przelicza podsumowanie ataków.

Pełny plan zaakceptowany przez usera (kontekst, algorytm, decyzje D1-D6) w:
`C:\Users\mlis\.claude\plans\przygotowujemy-si-du-ej-zmiany-resilient-shannon.md`
— ten HANDOFF jest jego operacyjnym odbiciem do śledzenia postępu.

## Zablokowane pliki / katalogi
- `app/models.py` — `+Unit.base_size`
- `app/services/costs/_engine.py` — `BASE_SIZE_MELEE_LIMITS`, bump `COST_ENGINE_VERSION`
- `app/services/costs/crowding.py` — NOWY, SSOT zniżki „tłok"
- `app/services/costs/quote.py` — wpięcie zniżki przed `_roster_unit_classification`
- `app/services/costs/role_totals.py` — param `melee_factors`
- `app/routers/rosters.py` — `_classification_map`, endpoint `/quote`, `_roster_unit_export_data`
- `app/routers/export.py` — battle-state: `melee_fighting_default`
- `app/routers/armies.py` — handler edycji jednostki (odczyt/zapis `base_size`)
- `app/templates/unit_form.html` — select rozmiaru podstawki
- `app/templates/roster_battle_state.html` — kontener pola „Walczące modele"
- `app/static/js/battle_state.js` — UI „Walczące modele" + przeliczenie podsumowania
- `app/static/js/payload_adapters.js` — nowe pola quote
- `app/db.py` — `_migrate_schema()` / `_initialize_unit_base_sizes()` (patrz korekta niżej)
- `data/szop.db` — po migracji (backup w `data/szop.db.bak-pre-base-size`)

## Blokuje / Blokowane przez
- **Blokuje:** (brak znanych w tej chwili)
- **Blokowane przez:** (brak — wątki kolekcja i demoralizacja-mag zamknięte 2026-07-14)

## Gałąź git
- **Branch:** `Rozwoj`
- **Base:** `main`

## Plan implementacji
*(Zsynchronizowany z planem zaakceptowanym przez usera. Oznaczaj kroki: `[ ]` TODO, `[~]` rozpoczęto, `[x]` sukces, `[!]` błąd/porzucone.)*

### Faza 1 — Data model + migracja + UI edycji — [x] ZAKOŃCZONA 2026-07-14
- [x] Krok 1.1: `Unit.base_size: Mapped[str]` (`String(10)`, `nullable=False`, `default="srednia"`) w `app/models.py:352`. Property `base_size_label` (+ `BASE_SIZE_LABELS` dict, models.py nie importuje z `costs` — unika cyklu). Stałe `BASE_SIZE_MELEE_LIMITS`, `DEFAULT_BASE_SIZE` + helper `base_size_melee_limits()` w `_engine.py`, re-eksportowane w `costs/__init__.py`. **`COST_ENGINE_VERSION` NIE bumpowany tutaj** — zostawiony `quote-v1`, bo bump razem z realną zmianą kształtu quote (Faza 3) złamałby exact-match fixture `tests/fixtures/quote_snapshots/*.json` już teraz bez powodu.
- [x] Krok 1.2: **Korekta planu — projekt NIE używa Alembic** (brak katalogu `alembic/`, `alembic.ini`). Rzeczywisty mechanizm: `app/db.py::_migrate_schema()` — hand-rolled idempotentne `ALTER TABLE ... ADD COLUMN` uruchamiane automatycznie w `init_db()` na `@app.on_event("startup")`. Ten sam wzorzec jak dla `typical_models`/`cast_difficulty`/etc. Dodano `_initialize_unit_base_sizes(connection)` (backfill wg D4, hero detection replikuje `unit_is_hero`: link do zdolności o slugu "bohater" via `ability_registry.ability_slug`, bez sprawdzania flagi default — zgodnie z runtime). `docs/architecture.md` wspomina "migracje Alembic" — to nieaktualne/aspiracyjne, nie odzwierciedla kodu; nie poprawiane w tym wątku (poza zakresem).
- [x] Krok 1.3: `unit_form.html` — `<select name="base_size">` w rzędzie z Jakość/Obrona/Wytrzymałość (kolumny `col-md-3`×4). `armies.py::_apply_unit_form_data` — nowy param `base_size`, walidacja przez `costs.BASE_SIZE_MELEE_LIMITS`, fallback do istniejącej wartości / `DEFAULT_BASE_SIZE`. Wpięte w oba call site `update_unit` (edit+save-as-new). `add_unit` (szybkie dodawanie z `army_edit.html`) NIE dostał selektora — celowo, dostaje bezpieczny fallback `srednia` przez ten sam mechanizm, user koryguje w pełnym edytorze.
- [x] Krok 1.4: Migracja zastosowana do `data/szop.db` (backup `data/szop.db.bak-pre-base-size` przed operacją). Rozkład: 14 duża / 18 mała / 19 średnia (51 jednostek), progi potwierdzone spot-checkiem (T=1→mala, T=9→duza). **`seeds/szop.db.seed` NIE zaktualizowany — celowo**: git log pokazuje że ten plik miał tylko 1 commit w historii (rebranding) i ŻADNA z ~7 poprzednich funkcji dodających kolumnę (typical_models, group_id, cast_difficulty, composed_models_json, ...) nigdy go nie re-snapshotowała. `_migrate_schema()` jest idempotentny i sam doprowadzi świeżą instalację ze starego seeda do aktualnego schematu przy pierwszym starcie — tak jak działa to dla wszystkich poprzednich kolumn.

### Faza 2 — Wektor kosztu wręcz per model + funkcje zniżki (bez wpięcia) — [x] ZAKOŃCZONA 2026-07-14
- [x] Krok 2.1: `app/services/costs/crowding.py` (NOWY). `melee_cost_per_model(unit, normalized_loadout, model_count, base_traits) -> list[float]` — sygnatura dołożyła `base_traits` (potrzebne przez `weapon_cost_components`, wołający musi je policzyć raz — patrz Faza 3). Tryb `per_model`: suma kosztu melee wszystkich wybranych broni, identyczna dla każdego modelu. Tryb `total`: instancje broni (waga=koszt melee, powtórzone wg liczby) sort malejąco, round-robin do `model_count` slotów (akumulacja gdy instancji więcej niż modeli). Brak jawnej broni (`has_explicit_weapons=False`) → `unit_default_weapons(unit)`, ZAWSZE per-model identyczny fallback niezależnie od trybu (mirror `calculate_roster_unit_quote`'s `has_explicit_weapons` guard w `quote.py`).
- [x] Krok 2.2: `melee_crowding_factors(vector, base_size) -> list[float]` — rankuje malejąco (remisy: stabilne po indeksie wejściowym), zwraca mnożniki 1.0/0.5/0.1 wg pozycji rankingu, **wyrównane do kolejności WEJŚCIOWEJ** (nie posortowanej) — `factors[i]` dotyczy `vector[i]`. `crowded_melee_total(vector, factors)` — ważona suma + round(2).
- [x] Krok 2.3: `tests/test_crowding.py` (NOWY, 15 testów) — granice progów, kolejność zachowana, remisy, fallback `base_size` nieznany/`None`, oba tryby `melee_cost_per_model`, round-robin, unknown weapon id. **Złapany i naprawiony realny bug podczas pisania testów** (patrz Notatki): fallback do default-weapons w trybie `total` zwracał same zera, bo gałąź `if mode_total:` czytała tylko z `total_counts`, a fallback wypełniał `per_model_counts`. Naprawione przez early-return z gałęzi fallback PRZED sprawdzeniem `mode_total`. `pytest -q`: 310/310 (295 + 15 nowych), zero regresji — oczekiwane, `crowding.py` jeszcze nie wpięty w `quote.py`/`role_totals.py`.

### Faza 3 — Wpięcie w quote: zniżka PRZED klasyfikacją (pojedynczy oddział) — [x] ZAKOŃCZONA 2026-07-14
- [x] Krok 3.1: `calculate_roster_unit_quote` ([quote.py](../../app/services/costs/quote.py)) — po istniejącej pętli melee/ranged dolicza `melee_vector = melee_cost_per_model(...)`, `melee_factors = melee_crowding_factors(...)`, `melee_bucket_effective = crowded_melee_total(...)`; `_roster_unit_classification` woła z `melee_bucket_effective` zamiast surowego `melee_bucket`.
- [x] Krok 3.2: `roster_unit_role_totals` ([role_totals.py](../../app/services/costs/role_totals.py)) — nowy param `melee_factors: Sequence[float] | None`. **Korekta projektu w trakcie:** `_aggregate_weapon_buckets` NIE ma fallbacku do domyślnej broni (w przeciwieństwie do `quote.py`) — nie mogłem więc wprost użyć `melee_cost_per_model` (ma fallback, wstrzyknąłby nieistniejący koszt). Rozwiązanie: wydzielony niżej-poziomowy `melee_vector_from_counts(weapons_counts, melee_cost_by_weapon, model_count, mode_total)` w `crowding.py` (bez fallbacku), na który `melee_cost_per_model` (publiczne API, Faza 2) teraz się deleguje. `role_totals.py` woła `melee_vector_from_counts` bezpośrednio na własnym `weapons_counts` + `_shared_weapon_components` (już policzone, zero dodatkowych `weapon_cost_components`). Brak przekazanego `melee_factors` ⇒ self-compute z własnego wektora + `unit.base_size` (poprawne dla samodzielnego oddziału; zewnętrzne `melee_factors` zarezerwowane pod Fazę 4 — pula grupowa). `_melee_effective` podstawiony w miejsce `weapon_buckets["melee"]` w OBU gałęziach `_compute_total` (wojownik/strzelec) — zniżka wchodzi PRZED mnożnikiem roli ×0.5 (D6).
- [x] Krok 3.3: Quote zwraca `melee_per_model`, `selected_total_full` (liczone ALGEBRAICZNIE: `selected_total + (melee_raw − melee_effective) × [1.0 jeśli wojownik inaczej 0.5]`, bez drugiego drogiego wywołania `roster_unit_role_totals`), `components.melee_crowding` (informacyjny, ujemny = oszczędność; NIE wchodzi do sumy `base+weapon+active+aura+passive` — jak `weapon_component` już dziś jest przybliżeniem, nie prawdziwym wkładem roli). Bump `COST_ENGINE_VERSION` → `quote-v2`.
- **Weryfikacja matematyczna ręczna** (patrz Notatki): zniżka daje DOKŁADNIE oczekiwane wartości co do grosza dla `duza`@6 modeli. `pytest -q`: 315/315 po aktualizacji 3 testów kontraktowych (nowy klucz `melee_crowding` w `components`) — ZERO zmian w istniejących wartościach liczbowych (wszystkie fixture'y mają `count <= 6`, poniżej progu `srednia` domyślnego).

### Faza 4 — Zniżka na poziomie grupy (bazowy oddział + bohaterowie) — [x] ZAKOŃCZONA 2026-07-14
- [x] Krok 4.1: Zamiast osobnej SSOT `compute_group_melee_factors` — pooling zaimplementowany BEZPOŚREDNIO w `_classification_map` jako nowa **Faza 1.5** (między istniejącą Fazą 1 i Fazą 2, patrz [rosters.py:256](../../app/routers/rosters.py)). Powód: pooling potrzebuje `melee_per_model` z quote'a KAŻDEGO członka (już zwracane od Fazy 3) + `group_members` mapping (już budowany w oryginalnej Fazie 2) — osobna funkcja w `crowding.py` musiałaby dublować tę orkiestrację bez realnej korzyści. `costs.melee_crowding_factors` (Faza 2, już eksportowane) użyte bezpośrednio na POOLED wektorze.
- [x] Krok 4.2: `calculate_roster_unit_quote` dostał nowy param `melee_factors_override: Sequence[float] | None` (keyword-only) — gdy podany, ZASTĘPUJE self-computed `melee_factors` (klasyfikacja I `roster_unit_role_totals` używają override). `_internal_roster_unit_quote` przekazuje go dalej. Faza 1.5 w `_classification_map`: dla każdej grupy z >1 członkiem — konkatenuje `melee_per_model` (kolejność deterministyczna: anchor/bazowy oddział pierwszy, bohaterowie posortowani po id), liczy `costs.melee_crowding_factors(pooled_vector, anchor.unit.base_size)`, TNIE z powrotem per-członek, RE-WOŁUJE `_internal_roster_unit_quote(member, loadout, melee_factors_override=slice)` — nadpisując `totals_by_id[member]` wynikiem uwzględniającym pulę grupową. Jednostki samodzielne (bez rodzica i bez dołączonych bohaterów, `len(member_ids)<=1`) pomijane — ich Faza-1 totals (self-computed) już poprawne, zero dodatkowej pracy.
- [x] Krok 4.3: WSZYSTKIE 4 call site `_classification_map` (linie 577, 1259, 1615, 1744 w `rosters.py` — w tym `/quote` endpoint grupowa gałąź i `recalculate_roster_costs`-adjacent kod) korzystają automatycznie, bez osobnych zmian — `_classification_map` jest jedynym miejscem tej logiki (SSOT), więc rozszerzenie jej wnętrza wystarczy.
- **Złapany i naprawiony bug w TEŚCIE (nie w produkcji)** — patrz Notatki: pierwsza wersja testu używała `{}` jako loadout, co ujawniło że `role_totals.py` nie ma fallbacku do domyślnej broni (znane z Fazy 3) — ale to NIGDY nie występuje w produkcji, bo `_roster_unit_loadout`/`_sanitize_loadout` zawsze rozwiązuje pusty `extra_weapons_json` przez `_default_loadout_payload` (jawny słownik broni domyślnej). Test naprawiony przez użycie `rosters._roster_unit_loadout(roster_unit)` zamiast `{}` — 3/3 przeszły, potwierdzając że **produkcyjne wpięcie było poprawne od razu**, błąd był wyłącznie w nierealistycznym fixture testu.
- `tests/test_group_melee_crowding.py` (NOWY, 3 testy): pula bohater+oddział z różnymi broniami (droższa broń bohatera "kradnie" pełnocenowy slot modelowi bazowego oddziału — dokładna weryfikacja liczbowa), limity z oddziału bazowego (nie z bohatera, nawet gdy bohater ma węższe limity), jednostka samodzielna nietknięta przez Fazę 1.5.
- **Performance:** zero nowych zapytań DB (cała logika na już wczytanych `RosterUnit.unit`/`melee_per_model` z quote'ów Fazy 1). Zmierzono osobno: testy silnika kosztów (102 testy w `test_crowding/test_roster_unit_quote/test_quote_endpoint_flow/test_passive_costs/test_weapon_costs/test_group_melee_crowding`) = 6.27s — brak spowolnienia. Wzrost całego `pytest -q` z ~10s do ~24s to WYŁĄCZNIE narzut testów `test_frontend_*` spawnujących procesy Node.js (niezwiązane z tym wątkiem, potwierdzone przez izolowane pomiary).

### Faza 5 — Stan bitewny: „Walczące modele" — [x] ZAKOŃCZONA 2026-07-14
- [x] Krok 5.1: `export.py` — nowy helper `_apply_melee_fighting_defaults(anchor_unit, group_members)` (wydzielony do osobnej, testowalnej funkcji zamiast inline w `roster_battle_state`) — `melee_fighting_default = min(limit1_z_base_size_ANCHORA, member.count)`, `0` gdy `count<=0`. Wołany w pętli budowy `battle_groups`, per grupa (limit z `entry.instance.unit.base_size` — bazowy/rodzicielski oddział, NIE bohater). `tests/test_export.py` +4 testy.
- [x] Krok 5.2: **Odkrycie:** `weapon_details` (via `_loadout_weapon_details` w rosters.py) nie niosło kosztu broni w ogóle, mimo że `weapon_options` (źródło) już go liczy (`costs.weapon_cost`) dla innych celów (podgląd w edytorze). Dołożono `"cost": option.get("cost")` do wynikowego dict — ZERO nowych wywołań `weapon_cost`, czysty reuse. Dla broni czysto wręcz (zasięg=0, dominujący przypadek) ten koszt jest DOKŁADNIE kosztem wręcz (bo ranged=0); dla broni Szturmowej (melee+ranged) to lekkie zawyżenie — akceptowalne, bo to heurystyka rankingu UI ("najdroższe instancje"), nie zapisywana wartość punktowa gry.
- [x] Krok 5.3: `roster_battle_state.html` — `data-melee-fighting-default="{{ member.melee_fighting_default | default(0) }}"` na `data-battle-unit`.
- [x] Krok 5.4: `battle_state.js` — `unitInitialState` +`meleeFighting` (z `data-melee-fighting-default`, fallback `initialModels`); nowy `clampMeleeFighting(unitState)` (self-healing clamp `[1, activeModels]`, wołany w `renderUnit` — wzorem istniejących defensywnych mutacji stanu jak init `eliminated`/`withdrawn` w trybie „Modele"); `migrateLegacyState` + `init()`'s backfill dla istniejącego (starszego) stanu localStorage bez tego pola. Licznik (−/wartość/+) renderowany jako PIERWSZA pozycja `data-attack-summary` TYLKO gdy `mode==="melee"` i `activeModels>1` (pomijane dla pojedynczego modelu). Nowy `selectTopMeleeInstances(filtered, budget)` — sortuje wpisy po `weapon.cost` malejąco (remisy: oryginalna kolejność), przycina `activeCount` aż do wyczerpania budżetu F — podawany do `groupAttacks` ZAMIAST pełnej listy `filtered`. Event handlery `data-melee-fighting-decrement/increment` obok istniejących `data-models-*`.
- **Konwencja stanu:** NIE ujednolicano z `eliminated`/tryb „Modele" (kolekcja) — to inny mechanizm (eliminacja wariantów, nie liczba walczących), żyją w różnych trybach toolbara (`models` vs `melee`), brak kolizji ani potrzeby współdzielenia struktury.
- **Weryfikacja w przeglądarce** (pełna, nie tylko syntax-check): `node --check` (via `.tools/node/...`, `node` brak w PATH Bash) → OK. Realna rozpiska `/rosters/13/battle-state` (Piechota, Gwardia): (1) `data-melee-fighting-default` poprawne dla WSZYSTKICH kart (mala→8 dla count>8, srednia/duza→odpowiednio, count dla mniejszych) — potwierdzone JS inspection; (2) tryb „Wręcz" na 20-modelowym oddziale → licznik „8 / 20", podsumowanie ataków POPRAWNIE ograniczone do F=8 najdroższych instancji (Piłomiecz×2 pełne + Laspistol×2 pełne + Lekka broń ręczna×4 z 18, suma=8, dokładnie zgodne z sortowaniem po koszcie); (3) klik [−] → 8→7, podsumowanie przeliczone (Lekka broń ręczna 4→3, najtańsza więc pierwsza do cięcia) — POPRAWNE; (4) reload strony → wartość 7 ZACHOWANA (localStorage) — „pamiętaj ostatnią wartość" potwierdzone; (5) hero 1-modelowy ("Starszy Szczurak") w trybie Wręcz → licznik NIEOBECNY w DOM — „pomijaj dla pojedynczego modelu" potwierdzone; (6) `read_console_messages` czyste przez całą sesję testową. `pytest -q`: 322/322 (bez zmian liczbowych, tylko +4 testy `test_export.py`).

### Faza 6 — Weryfikacja end-to-end (Definition of Done)
- [x] `pytest -q` (baseline 2026-07-14: 291 passed — zanotowany przed startem; po Fazach 1-5: 322 passed)
- [x] Smoke test JS (`battle_state.js`, `unit_form.html` dotknięte) — patrz weryfikacje w Fazach 1 i 5, plus re-weryfikacja po `/simplify` niżej.
- [x] Call-site check — wykonany naturalnie podczas refaktoringu `/simplify` (musiałem znaleźć WSZYSTKIE miejsca wołające `melee_cost_per_model`, `_aggregate_weapon_buckets`, `weapon_buckets["melee"]` żeby bezpiecznie je zmienić). Brak innych callerów poza już zidentyfikowanymi.
- [!] `payload_adapters.js` — nie dotyczy: nowe pola quote (`selected_total_full`, `melee_per_model`, `components.melee_crowding`) NIE są jeszcze przekazywane przez HTTP endpoint `/quote` (`rosters.py::quote_roster_unit` explicite wymienia zwracane klucze, nie przekazuje surowego dict quote'a) — świadoma decyzja z Fazy 3, ekspozycja we froncie to zakres przyszłej fazy (Faza 5/6 UI dla roster editor, nie Stanu Bitewnego). `payload_adapters.js` nie wymaga zmian w TYM wątku.
- [x] `tests/test_frontend_*.py` — parity: przeszły bez zmian (322/322 obejmuje cały katalog `tests/`).
- [x] **`/simplify`** — 4 agenty (altitude/reuse/simplification/efficiency) równolegle na pełnym diffie. 3 z 4 agentów NIEZALEŻNIE zbieżnie wskazały tę samą najważniejszą redundancję (patrz Notatki). Naprawione: (1) `quote.py` przestał redundantnie re-rozwiązywać bronie i wołać `weapon_cost_components` drugi raz — teraz buduje `melee_cost_by_weapon`/`weapon_counts` w ISTNIEJĄCEJ pętli i woła `melee_vector_from_counts` bezpośrednio (mirror wzorca już używanego w `role_totals.py`), martwy `melee_bucket` usunięty; (2) `role_totals.py::_aggregate_weapon_buckets` przestał liczyć nieużywany klucz `"melee"` (biegło 2x per quote, raz na rolę, na nic) — przemianowane na `_aggregate_ranged_total`, zwraca `float` zamiast `dict`; (3) `crowding.py::melee_cost_per_model` — usunięty zbędny dual-dict (`per_model_counts`/`total_counts` + ternary) na rzecz jednego `counts` dict (mode_total jest stały w pętli); (4) `battle_state.js` — nowy `counterButton()` helper, `renderMeleeFightingCounter` przestał duplikować budowę przycisków. Pominięte świadomie: scalenie Faz 1.5/2 w `_classification_map` (brak zysku wydajnościowego, ryzyko dla przetestowanej logiki grupowej), przeniesienie `weapon_details.cost` poza współdzielony DTO (dotyka print/xlsx/battle-state, poza zakresem), poczwórna migracja-helper w `db.py` (wzorzec już istniał w 3 funkcjach przed tym wątkiem). Po każdej naprawie: `pytest -q` (322/322, ZERO zmian numerycznych — potwierdza że to były czyste refaktory) + re-weryfikacja w przeglądarce dla JS (identyczny HTML output przed/po).
- [ ] `/review` (diff >50 linii, hot path `/quote`, SSOT — na pewno dotyczy)
- [!] `/security-review` — nie dotyczy: brak zmian w auth/uprawnieniach (tylko koszt + nowa kolumna jednostki edytowana przez istniejący, już autoryzowany handler)
- [x] Re-run `pytest -q` po `/simplify` — 322/322 po każdej z 4 napraw, potwierdzone.
- [ ] Sprawdzić `docs/PERFORMANCE.md` przed/po (regresja = blocker)
- [ ] Diff review przed commitem

## Pliki dotknięte
- `app/models.py` — `+Unit.base_size`, `+BASE_SIZE_LABELS`, `+Unit.base_size_label` property.
- `app/services/costs/_engine.py` — `+BASE_SIZE_MELEE_LIMITS`, `+DEFAULT_BASE_SIZE`, `+base_size_melee_limits()`.
- `app/services/costs/__init__.py` — re-export nowych symboli.
- `app/db.py` — `+_initialize_unit_base_sizes()`, wpięcie w `_migrate_schema()` bloku `units`, `+from types import SimpleNamespace`.
- `app/routers/armies.py` — `_apply_unit_form_data(..., base_size=...)`, `update_unit(base_size: str = Form(...))` (oba call site: new + save).
- `app/templates/unit_form.html` — select `base_size` w rzędzie statystyk.
- `tests/test_base_size_migration.py` — NOWY, migracja+backfill (9 jednostek testowych, w tym 3 hero, idempotencja).
- `tests/test_weapon_primary_flags.py` — +3 testy `_apply_unit_form_data` (valid/invalid/missing base_size).
- `data/szop.db` — migracja zastosowana (backup `data/szop.db.bak-pre-base-size`).
- `app/services/costs/crowding.py` — NOWY: `melee_cost_per_model`, `melee_crowding_factors`, `crowded_melee_total`, `melee_vector_from_counts` (dołożony w Fazie 3, bez fallbacku do domyślnej broni).
- `app/services/costs/__init__.py` — re-export nowych funkcji `crowding.py` (bezpośrednio z `.crowding`, nie przez `_engine` stub — moduł nie należy do oryginalnej numeracji Sekcji 1-10, więc nie potrzebuje warstwy kompatybilności).
- `app/services/costs/role_totals.py` — param `melee_factors`, wpięcie `_melee_effective` w `_compute_total` (obie gałęzie roli).
- `app/services/costs/quote.py` — wektor/factors/`melee_bucket_effective` przed klasyfikacją, `melee_factors` przekazane do `roster_unit_role_totals`, nowe pola `selected_total_full`/`melee_per_model`/`components.melee_crowding`.
- `app/services/costs/_engine.py` — `COST_ENGINE_VERSION` → `quote-v2`.
- `tests/test_crowding.py` — 20 testów (15 z Fazy 2 + 5 nowych dla `melee_vector_from_counts`).
- `tests/test_roster_unit_quote.py`, `tests/test_frontend_compute_total_cost_regression.py` — 3 testy kontraktowe zaktualizowane (nowy klucz `melee_crowding`, nowe pola zero-contract).
- `app/services/costs/quote.py` — `+melee_factors_override` param w `calculate_roster_unit_quote`.
- `app/routers/rosters.py` — `_internal_roster_unit_quote(+melee_factors_override)`, `_classification_map` +Faza 1.5 (pooling grupowy).
- `tests/test_group_melee_crowding.py` — NOWY, 3 testy end-to-end (baza danych w pamięci, prawdziwe `models.Unit`/`RosterUnit`).
- `app/routers/rosters.py` — `_loadout_weapon_details` +pole `"cost"` w wynikowym dict (reuse `weapon_options`' już policzonego `costs.weapon_cost`).
- `app/routers/export.py` — nowy `_apply_melee_fighting_defaults()`, wołany w budowie `battle_groups`.
- `app/templates/roster_battle_state.html` — `+data-melee-fighting-default`.
- `app/static/js/battle_state.js` — `unitInitialState`/`migrateLegacyState`/`init()` +`meleeFighting`; nowe `clampMeleeFighting`, `selectTopMeleeInstances`, `renderMeleeFightingCounter`; `renderUnit` wpięcie w tryb „Wręcz"; nowe event handlery `data-melee-fighting-{decrement,increment}`.
- `tests/test_export.py` — +4 testy `_apply_melee_fighting_defaults`.

## Hipotezy / pytania otwarte
- [x] ROZWIĄZANE (Faza 2/3, testy `test_crowding.py`): Heurystyka przydziału w trybie `total` (round-robin po sorcie malejącym) — 5 testów dedykowanych (`melee_cost_per_model`/`melee_vector_from_counts` total-mode), w tym round-robin z akumulacją >1 instancji na model.
- [x] ROZWIĄZANE (Faza 3, weryfikacja ręczna + testy): Kumulacja mnożników — zniżka „tłok" × komponent melee PRZED mnożnikiem roli ×0.5, potwierdzone co do grosza na jednostce `duza`@6 modeli.
- [x] ROZWIĄZANE (Faza 3, brak zmian potrzebnych): Oddziały komponowane (kolekcja, `composed_models_json`) są wyceniane jako `mode="total"` — `melee_cost_per_model`'s total-mode branch obsługuje je bez osobnej ścieżki, potwierdzone przez brak regresji w `tests/test_collection_match.py` (15/15 nadal zielone).
- Jeszcze do zrobienia (Faza 5/6): czy `composed_models_json` oddziały w Stanie Bitewnym (tryb „Modele") powinny też pokazywać „Walczące modele" per-wariant, czy tylko na poziomie całego oddziału? Do rozstrzygnięcia przy implementacji Fazy 5.

## Jak zweryfikować
```bash
python -m pytest -x --tb=short -q
# nowe testy: costs/crowding.py (wektor, granice limitów, grupa z bohaterem)
# tests/test_frontend_*.py (parity)
# smoke manualny: edytor jednostki (zapis base_size), rozpiska (koszt dużego oddziału),
#   Stan bitewny → Wręcz → Walczące modele zmienia podsumowanie ataków, ukryte dla 1 modelu
```

## Decyzje
- 2026-07-14: `base_size` per-oddział (nie per-armia), trzymany jak atrybut podstawowy (quality/defense/toughness), nie różnica wariantu — uzasadnienie: różne typy oddziałów w tej samej armii mają różne podstawki (piechota vs potwór).
- 2026-07-14: Zniżka dotyczy WYŁĄCZNIE kosztu broni wręcz (nie bazy, nie zdolności, nie broni dystansowej).
- 2026-07-14: Zniżka liczona PRZED klasyfikacją Wojownik/Strzelec i na nią wpływa — obniżony melee wchodzi do porównania melee vs dystans.
- 2026-07-14: Migracja backfill z wytrzymałości (T≤2/3-5/≥6), bohaterowie liczeni jak dla floor(T/2). Brak oddziałów T=5 w bazie.
- 2026-07-14: „Walczące modele" przelicza podsumowanie ataków (nie tylko licznik informacyjny).

## Notatki / odkrycia w trakcie
- 2026-07-14: Weryfikacja planu po zamknięciu blokujących wątków — kod niezmieniony od pierwszej eksploracji (`b061532`), kotwice OK, `base_size` nigdzie nie istnieje (brak konfliktu). Wątek kolekcja dołożył `app/services/collection_match.py`: oddziały komponowane liczone jako `mode="total"` (heurystyka total-mode z Fazy 2 powinna je objąć bez osobnej ścieżki); Stan bitewny ma już tryb „Modele" (`collection_groups`, `eliminated`) — „Walczące modele" idzie w tryb „Wręcz", brak kolizji, ale ujednolicić konwencję stanu/localStorage.
- 2026-07-14: Baseline pytest przed startem prac: `291 passed, 0 failed` (`python -m pytest -x --tb=short -q`).
- 2026-07-14: **Korekta planu — brak Alembic w projekcie.** Zobacz Krok 1.2 wyżej. Dla kolejnych faz/wątków: jeśli plan mówi "migracja Alembic", zawsze sprawdź `app/db.py::_migrate_schema()` jako rzeczywisty mechanizm.
- 2026-07-14: `COST_ENGINE_VERSION` NIE bumpowany w Fazie 1 — zostaje `quote-v1` dopóki kształt `calculate_roster_unit_quote` faktycznie się nie zmieni (Faza 3). Bump przedwczesny łamie `tests/test_quote_endpoint_flow.py::_assert_snapshot` (exact-match na fixture JSON) bez żadnej korzyści.
- 2026-07-14: Smoke test w przeglądarce (`preview_start` z `url` do już działającego `localhost:8000` — port zajęty przez istniejący proces uvicorn `--reload`, nie startowano nowego). **Tool quirk:** `read_page` z `filter: "interactive"` na `unit_form.html` urywa drzewo po ok. 30 refs (przed sekcją "Dostępne bronie" / weapon-picker), niezależnie od `max_chars`/`depth` — podejrzenie: wczesne zatrzymanie na kontenerze `aria-hidden="true"` (ukryty `<select class="weapon-picker-select d-none" aria-hidden="true">`). Obejście: `filter: "all"` + `depth: 50` zwraca pełne drzewo z poprawnymi refs (potwierdzone: `button "Zapisz" [ref_558]` znaleziony i klikalny). `computer{action:"screenshot"}` na tej stronie konsekwentnie timeout'ował (2×) — nieużywany, zweryfikowano tekstowo (`get_page_text`/`read_page`) zamiast tego. Warto pamiętać przy przyszłych smoke testach stron z weapon-pickerem.
- 2026-07-14: Pełny round-trip zweryfikowany na żywo: zalogowano jako admin, `/armies/1/units/1/edit` (Piechota, T=1) → select pre-zaznaczony "Mała" (zgodnie z backfillem) → zmieniono na "Duża" → submit → redirect na `/armies/1` (sukces, brak błędu walidacji) → potwierdzone bezpośrednio w DB: `base_size='duza'`. Następnie przywrócono do poprawnej wartości migracji (`mala`) — zmiana była tylko testem zapisu.
- 2026-07-14: Backup `data/szop.db.bak-pre-base-size` pozostawiony w `data/` (gitignored, nieszkodliwy) jako punkt przywrócenia sprzed migracji — do ręcznego usunięcia przez usera jeśli niepotrzebny.
- 2026-07-14: **Odkrycie w trakcie Fazy 3 — `role_totals.py` nie ma fallbacku do domyślnej broni.** `_aggregate_weapon_buckets` zwraca melee=0 gdy `weapons_counts` jest puste (brak jawnego wyboru broni), w PRZECIWIEŃSTWIE do `quote.py`'s `has_explicit_weapons` fallback (uzupełnia `unit_default_weapons`). To PRZEDISTNIEJĄCA rozbieżność (nie moja regresja) — `melee_cost_per_model` (Faza 2, ma fallback) nie mógł być użyty wprost w `role_totals.py` bez wstrzyknięcia nieistniejącego kosztu. Rozwiązanie: wydzielony `melee_vector_from_counts` (bez fallbacku) jako wspólny dolny poziom, `melee_cost_per_model` deleguje do niego. Jeśli przyszła faza kiedyś ujednolica te dwie ścieżki (poza zakresem tego wątku) — pamiętać o tej rozbieżności.
- 2026-07-14: **Złapany i naprawiony bug podczas pisania testów** (Faza 2→3 granica): fallback do domyślnej broni w `melee_cost_per_model` przy `mode="total"` zwracał same zera (gałąź `if mode_total:` czytała tylko `total_counts`, fallback wypełniał `per_model_counts`). Naprawione przez early-return z gałęzi fallback. Testy złapały to OD RAZU (2 failed na 15) — potwierdza wartość testowania Fazy 2 osobno, PRZED wpięciem w quote.
- 2026-07-14: **Weryfikacja ręczna matematyki zniżki** (poza pytest, `python -c`) na jednostce `duza` (limit 2/4) z 6 modelami: dyskontowany koszt melee = 2×pełny + 2×pełny×0.5 + 2×pełny×0.1, spadek kosztu wojownik dokładnie = 16.32 (zweryfikowane co do grosza), strzelec = wojownik − melee_effective×0.5 (dokładnie). `pytest -q` po całej Fazie 3: 315/315, ZERO zmian numerycznych w istniejących testach (wszystkie fixture'y `count<=6`, poniżej domyślnego progu `srednia`=6/8) — 3 testy kontraktowe zaktualizowane tylko o nowy klucz `melee_crowding` (celowa, oczekiwana zmiana kontraktu, nie regresja).
- 2026-07-14: `tests/fixtures/quote_snapshots/{quote_per_model,quote_massive_total}.json` nadal mają `"cost_engine_version": "quote-v1"` — NIE zaktualizowane, celowo. Są to statyczne dane WEJŚCIOWE dla testu JS-adaptera (`test_frontend_payload_adapters.py`), nie oracle porównywany z żywym outputem backendu — sprawdzają że adapter obsługuje STARSZY kształt payloadu (backward-compat), więc bump wersji backendu ich nie dotyczy.
- 2026-07-14: **Potwierdzenie z Fazy 4 — "brak fallbacku" w `role_totals.py` (notatka wyżej) jest NIEOSIĄGALNE w produkcji.** Pierwsza wersja `tests/test_group_melee_crowding.py` użyła `{}` jako loadout w `_classification_map`, co dało `totals_by_id` IDENTYCZNE przed/po poolingu (pozorny brak działania Fazy 1.5!). Debug wykazał: `_roster_unit_loadout`/`_sanitize_loadout` (rosters.py) NIGDY nie zwraca pustego `weapons={}` dla jednostki z bronią domyślną — `if not payload: return defaults`, a `_default_loadout_payload` zawsze wypełnia `weapons` z `default_count` każdej opcji. Pusty loadout to WYŁĄCZNIE sztuczny scenariusz testowy (jak mój manualny `python -c` w Fazie 3 — tam też dawał złudnie "brak dyskonta" dla pierwszego, błędnego sprawdzenia). **Wniosek dla przyszłych testów silnika kosztów:** zawsze buduj loadout przez `rosters._roster_unit_loadout(roster_unit)` (lub jawny `{"mode":..., "weapons": {...}}`), nigdy `{}`, inaczej testy cicho nie wykrywają regresji w ścieżce broni.
- 2026-07-14: **Performance po Fazie 4:** zero nowych zapytań DB (potwierdzone architekturalnie — cała Faza 1.5 operuje na `melee_per_model` z już policzonych quote'ów). Zmierzone osobno: 102 testy silnika kosztów = 6.27s (bez zmian). Wzrost pełnego `pytest -q` z ~10s do ~24s w trakcie sesji to WYŁĄCZNIE narzut testów `test_frontend_*` spawnujących Node.js (potwierdzone izolowanym pomiarem — 22.70s bez `test_group_melee_crowding.py` wcale).
- 2026-07-14: **`/simplify` — 3 z 4 równoległych agentów (altitude/reuse/simplification) NIEZALEŻNIE wskazały tę samą redundancję**: `quote.py`'s `calculate_roster_unit_quote` wołał `melee_cost_per_model` (Faza 3), który wewnętrznie od nowa rozwiązywał `weapon_by_id` i wołał `weapon_cost_components` — DOKŁADNIE to co ISTNIEJĄCA pętla melee/ranged w tej samej funkcji już robiła linijkę wyżej. `melee_bucket` (stary scalar, zastąpiony przez `melee_bucket_effective` w Fazie 3) stał się martwym kodem. Poprawka: przebudowana pętla buduje `melee_cost_by_weapon`/`weapon_counts` i woła nisko-poziomowy `melee_vector_from_counts` bezpośrednio — mirror wzorca który `role_totals.py` już poprawnie stosował od Fazy 3 (`_shared_weapon_components`). **Wniosek:** przy dodawaniu nowej funkcji SSOT do istniejącej, już-zoptymalizowanej funkcji — sprawdź czy wywołanie wyższo-poziomowego, self-contained API (tu: `melee_cost_per_model`) nie duplikuje pracy którą CALLER już wykonał; czasem trzeba sięgnąć po niższy poziom abstrakcji (tu: `melee_vector_from_counts`) właśnie DLATEGO że wyższy poziom jest "zbyt samowystarczalny" dla kontekstu który już ma połowę danych. Dodatkowo naprawione: martwy klucz `"melee"` w `_aggregate_weapon_buckets` (liczony 2x/quote, nieużywany), zbędny dual-dict w `melee_cost_per_model`, duplikacja budowy przycisków w `battle_state.js`. Wszystkie 4 poprawki zweryfikowane: `pytest -q` 322/322 po KAŻDEJ (zero zmian numerycznych — czyste refaktory), JS dodatkowo zweryfikowany w przeglądarce (identyczny HTML output przed/po na `/rosters/13/battle-state`).
